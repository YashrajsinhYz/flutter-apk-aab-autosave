# flutter-apk-aab-autosave
Auto‑save Flutter release APK/AAB files with versioned names into a build_outputs/ folder using a tiny Gradle hook. No more manual renaming or copying after every flutter build.
